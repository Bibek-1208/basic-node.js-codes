/*
Primitive (Basic)-means single value and immutable (cannot be changed directly).
String, Number, Boolean, Undefined, Null, BigInt, Symbol

Non-Primitive (Reference)-Used to store data in key-value pairs.
Object, Array, Function */


        //string  
        let name = "Bibek";
        let greet = 'Hello';
        let message = `Welcome ${name}!`; // Template literal
        console.log(message)


        // number
        let age = 20;
        let price = 99.99;
        let infinityNum = Infinity;
        let notANum = NaN; // Not a Number



        //Type Checking- You can check the data type using typeof operator.
             
                       let marks=45;
                       console.log(typeof(marks)) ;


 








